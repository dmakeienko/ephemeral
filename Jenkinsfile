pipeline {
    agent any {
        stages {
            stage('Check prerequisites') {
                steps {
                    sh "python --version"
                }
            }
        }
    }
}
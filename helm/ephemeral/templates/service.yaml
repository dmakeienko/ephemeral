apiVersion: v1
kind: Service
metadata:
  name: {{ include "ephemeral.fullname" . }}
  labels:
    {{- include "ephemeral.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ephemeral.selectorLabels" . | nindent 4 }}
